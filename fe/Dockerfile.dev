# 개발 모드 실행용 Dockerfile
FROM node:18 AS dev

# 작업 디렉토리 설정
WORKDIR /app

# package.json 및 lock 파일 복사
COPY package*.json ./

# 안정적인 의존성 설치
RUN npm ci

# 소스 코드 복사
COPY . .

# Vite 개발 서버 실행
CMD ["npm", "run", "dev"]